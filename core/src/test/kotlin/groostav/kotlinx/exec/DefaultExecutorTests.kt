package groostav.kotlinx.exec

import org.junit.Test

class DefaultExecutorTests {

    @Test fun todo(): Unit = TODO("get telemetry from the DefaultExecutor, to see how much time it actually spent chrunching")
}