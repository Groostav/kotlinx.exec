package groostav.kotlinx.exec

import org.junit.Test

class WindowsFixture {

    @Test
    fun todo(): Unit = TODO(
            """
                there are a huge number of code paths through windows.kt and its class WindowsProcessControl.kill,
                those should be excercised here.

                Also note: I've re-implemented the graceful kill. I dont know if it works.
            """.trimIndent()
    )
}